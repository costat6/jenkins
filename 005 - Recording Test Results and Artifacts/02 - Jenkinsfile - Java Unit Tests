pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('tests') {
            steps {
                sh "mvn test"
            }
        }
    }
	post {
        always {
            junit 'target/surefire-reports/*.xml' 
        }
    }
}